<article>
  <section class="filters">
    <form id="searchForm" role="form" action="/;jsessionid=61B10B6F9C08053BE8BC9B7447D2DFD5" method="post">
      <ul>
        <li>
          <label for="Buildings">
            <div class="custom">
            {{view "select"
              content=buildings
              optionValuePath="content.id"
              optionLabelPath="content.name"
              selection=selectedBuilding
              prompt="Any building"}}
            </div>
          </label>
        </li>
        <li class="hidden">
          <label for="DateTime">
            <input id="DateTime" type="datetime-local">
          </label>
        </li>
        <li class="hidden">
          <label for="Attendees">
            {{input type="number" placeholder="# of people" value=seating}}
          </label>
        </li>
        <li>
          {{input type="checkbox" name="seating" checked=5 viewName="seating"}}
          <label {{bind-attr for="view.seating.elementId"}}>5 <span class="icon-person"></span></label>
          {{input type="checkbox" name="seating" checked=10 viewName="seating"}}
          <label {{bind-attr for="view.seating.elementId"}}>10 <span class="icon-person"></span></label>
          {{input type="checkbox" name="seating" checked=15 viewName="seating"}}
          <label {{bind-attr for="view.seating.elementId"}}>15 <span class="icon-person"></span></label>
        </li>
        <li>
          {{input type="checkbox" name="display" checked=display viewName="display"}}
          <label {{bind-attr for="view.display.elementId"}}><span class="icon-projector"></span> display</label>
        </li>
        <li>
          {{input type="checkbox" name="phone" checked=phone viewName="phone"}}
          <label {{bind-attr for="view.phone.elementId"}}><span class="icon-phone"></span> Conference Phone</label>
        </li>
        <li class="hidden">
          <label for="Search">
            <input type="submit" id="Search" value="Search" class="button" />
          </label>
        </li>
      </ul>
    </form>
  </section>
  <section class="results">
    <h2>{{filteredRooms.length}} Rooms available</h2>
    {{#each building in packagedRooms}}
    <h4>{{building.name}}</h4>
    <ul>
      {{#each room in building.rooms}}
        <li>
          <h3>{{room.type}} {{room.roomNumber}}</h3>
          {{#if room.display}}
            <span class="icon-projector"></span>
          {{/if}}
          {{#if room.phone}}
            <span class="icon-phone"></span>
          {{/if}}
          {{room.seating}} <span class="icon-person"></span>
          {{#if room.hasUsageRestrictions}}
            <br/>
            <span class="icon-warning"></span> {{room.manager}}, {{room.usageRestrictions}}
          {{/if}}
          <br/>
          <em>{{room.equipment}}</em>
        </li>
      {{/each}}
    </ul>
    {{/each}}
  </section>
</article>